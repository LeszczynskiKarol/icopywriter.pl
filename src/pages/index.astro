---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionHeading from '../components/SectionHeading.astro';

// ── Photo ──
const photo = 'https://s3.eu-north-1.amazonaws.com/piszemy.com.pl/karol_leszczynski_copywriter.jpeg';

// ── Services ──
const services = [
  {
    num: '01',
    title: 'Copywriting sprzedażowy',
    desc: 'Teksty, które prowadzą czytelnika do działania — landing page, oferty, strony usługowe, maile sprzedażowe. Każde zdanie ma cel. Każde CTA jest przetestowane. Piszę copy, które konwertuje, bo rozumiem psychologię decyzji i architekturę perswazji.',
    tags: ['Landing page', 'Oferty', 'Strony usługowe', 'Email marketing']
  },
  {
    num: '02',
    title: 'SEO copywriting',
    desc: 'Artykuły, podstrony i klastry treści, które budują widoczność w Google. Nie piszę pod algorytm — piszę dla ludzi z wiedzą o tym, jak działa algorytm. Research fraz, analiza SERP, optymalizacja on-page, linkowanie wewnętrzne. Teksty, które rankują i czyta się z przyjemnością.',
    tags: ['Artykuły blogowe', 'Pillar pages', 'Content clusters', 'Opisy kategorii']
  },
  {
    num: '03',
    title: 'Content marketing',
    desc: 'Strategia, kalendarz, realizacja. Tworzę treści, które budują autorytet marki i generują leady w długim terminie — artykuły eksperckie, bazy wiedzy, newslettery, ebooki. Content, który pracuje na Ciebie miesiącami po publikacji.',
    tags: ['Strategia contentowa', 'Bazy wiedzy', 'Newslettery', 'Social media']
  },
  {
    num: '04',
    title: 'Publikacje & skład tekstu',
    desc: 'To moja unikalna przewaga. Nie tylko piszę — składam typograficznie w LaTeX i Quarto, renderuję do PDF print-ready, EPUB, HTML. Ebooki, podręczniki, katalogi, materiały szkoleniowe, white papers — od słowa po gotowy produkt wydawniczy.',
    tags: ['Ebooki', 'Katalogi', 'White papers', 'Materiały szkoleniowe'],
    accent: true
  },
  {
    num: '05',
    title: 'Redakcja & korekta',
    desc: 'Poprawiam, doszlifowuję, wycinam zbędne. Redakcja merytoryczna i stylistyczna, korekta językowa, proofreading. Każdy tekst przechodzi minimum dwie rundy — bo pierwszy draft nigdy nie jest ostatecznym produktem.',
    tags: ['Redakcja merytoryczna', 'Korekta językowa', 'Proofreading', 'Style guide']
  },
  {
    num: '06',
    title: 'Dokumentacja techniczna',
    desc: 'Instrukcje obsługi, dokumentacja API, bazy wiedzy, help center, SOP. Tłumaczę język inżyniera na język użytkownika. Technical writing z docs-as-code workflow — Git, CI/CD, Markdown, rendering automatyczny.',
    tags: ['Instrukcje', 'Dokumentacja API', 'Help center', 'SOP']
  }
];

// ── Process ──
const process = [
  { step: 'Brief & research', desc: 'Zaczynam od pytań, nie od pisania. Cel biznesowy, odbiorca, ton, konkurencja. Research fraz, analiza SERP, audyt istniejących treści. Im lepszy brief, tym lepszy tekst.' },
  { step: 'Struktura & wireframe', desc: 'Przed pierwszym zdaniem tworzę szkielet — nagłówki, sekcje, flow argumentacji. W przypadku stron: wireframe tekstowy. W przypadku publikacji: makieta typograficzna. Struktura to 60% sukcesu.' },
  { step: 'Copywriting & redakcja', desc: 'Piszę pierwszy draft, redaguję, wycinam. Potem redaguję jeszcze raz. Szlifuję nagłówki, CTA, przejścia między sekcjami. Każdy tekst przechodzi minimum 2 rundy wewnętrznej redakcji, zanim go zobaczysz.' },
  { step: 'Skład & finalizacja', desc: 'Tekst w Wordzie? Jasne. Ale jeśli potrzebujesz gotowego produktu — składam w LaTeX, renderuję PDF, projektuję layout, przygotowuję do druku. Lub wdrażam na stronę. Od słowa do gotowego deliverable.' },
  { step: 'Feedback & poprawki', desc: 'Przesyłam do akceptacji, zbieram feedback, wprowadzam poprawki. Bez limitu „rund" w rozsądnym zakresie — bo celem jest Twoje zadowolenie, nie zamknięcie ticketa.' }
];

// ── What sets me apart ──
const advantages = [
  {
    title: 'End-to-end: od słowa po gotowy produkt',
    desc: 'Większość copywriterów oddaje tekst w Google Docs. Ja dostarczam gotowe produkty — ebooki ze składem typograficznym, katalogi print-ready, strony z wdrożonymi tekstami, bazy wiedzy z architekturą informacji. Jeden człowiek zamiast łańcucha freelancerów.',
    icon: '⬡'
  },
  {
    title: 'Copywriting + programowanie',
    desc: 'Buduję strony w Astro, Next.js i React. Wdrażam landing page, docs-as-code, pipeline renderingu. Rozumiem technologię, bo ją tworzę — i dlatego piszę lepsze teksty techniczne, lepsze UX copy i lepsze specyfikacje niż ktoś, kto nigdy nie otworzył terminala.',
    icon: '⟐'
  },
  {
    title: 'Skład typograficzny na poziomie wydawnictwa',
    desc: 'LaTeX, Quarto, InDesign, Pandoc. Składam książki, podręczniki, zeszyty ćwiczeń, katalogi. Perfekcyjny kerning, dzielenie wyrazów, cross-referencje, bibliografie, indeksy — to nie Canva, to rzemiosło typograficzne. 11 zeszytów ćwiczeń w jednym projekcie? Zrobione.',
    icon: '◈'
  },
  {
    title: 'Dane, nie intuicja',
    desc: 'Research fraz w Ahrefs i Semrush, analiza SERP, audyt konkurencji. Nie piszę „bo tak mi się wydaje" — piszę na podstawie danych o tym, czego szukają ludzie, co rankuje i dlaczego. Potem mierzę efekty i optymalizuję.',
    icon: '◇'
  }
];

// ── Portfolio highlights ──
const portfolio = [
  { category: 'Materiały szkoleniowe', title: '11 zeszytów ćwiczeń dla wydawnictwa', result: '1500+ stron' },
  { category: 'Platforma EdTech', title: 'MaturaPolski.pl — baza pytań i opracowań lektur', result: 'Setki pytań + opracowania' },
  { category: 'Ebook', title: 'Jak napisać pracę magisterską — poradnik 250 str.', result: 'Bestseller w kategorii' },
  { category: 'E-commerce', title: 'Opisy produktów — sklep z silnikami elektrycznymi', result: '+40% sprzedaży' },
  { category: 'Katalog B2B', title: 'Katalog produktowy aplikacji medycznych VR', result: '71 stron, print-ready' },
  { category: 'Dokumentacja', title: 'Instrukcja obsługi medycznych stołów anatomicznych', result: '55 stron, technical writing' },
  { category: 'Treści na stronę', title: 'UniaTorun.pl — 17 podstron + 4 SEO landing pages', result: '17 podstron z SEO' },
  { category: 'Baza wiedzy', title: 'Prace-Magisterskie.pl — 30+ artykułów eksperckich', result: '30+ artykułów' }
];

// ── Testimonial / Trust ──
const trustPoints = [
  '15+ lat w copywritingu i digital marketingu',
  'Ponad 1500 stron materiałów szkoleniowych w jednym projekcie',
  'Wzrost sprzedaży o 40% po nowych opisach produktów',
  'Full-stack: copy + design + skład + development',
  'LaTeX, Quarto, Astro, Next.js, React, Node.js, AWS'
];

// ── FAQ ──
const faqs = [
  { q: 'Czym zajmuje się copywriter?', a: 'Copywriter to specjalista od tworzenia tekstów, które realizują cele biznesowe — sprzedają, informują, edukują, budują wizerunek. Dobry copywriter nie tylko pisze — najpierw analizuje grupę docelową, bada konkurencję, planuje strukturę tekstu, a potem szlifuje każde zdanie, aż osiągnie zamierzony efekt. To połączenie strategii marketingowej, psychologii perswazji i rzemiosła językowego.' },
  { q: 'Ile kosztuje copywriter?', a: 'Ceny zależą od rodzaju tekstu, złożoności tematu i zakresu prac. Orientacyjnie: artykuł blogowy 600–2000 zł, teksty na podstronę 500–2000 zł, opisy produktów 50–300 zł/szt., ebook 3000–15 000 zł. Wycenę konkretnego projektu przygotowuję w ciągu 24 godzin — bezpłatnie i bez zobowiązań.' },
  { q: 'Czym się różnisz od innych copywriterów?', a: 'Łączę copywriting z profesjonalnym składem typograficznym (LaTeX, Quarto) i programowaniem (Astro, Next.js, React). Nie oddaję tekstu w Wordzie — dostarczam gotowe produkty: ebooki ze składem, katalogi print-ready, strony z wdrożonymi tekstami. Jeden człowiek zamiast łańcucha freelancerów: copy → redakcja → skład → wdrożenie.' },
  { q: 'Jak wygląda współpraca?', a: 'Zaczynamy od briefu — cel, odbiorca, zakres, termin. Przygotowuję wycenę w 24h. Po akceptacji: research, struktura, copywriting, redakcja, skład/wdrożenie, feedback, poprawki, finalizacja. Komunikacja mailowa lub Slack. Płatność: 50% zaliczki, 50% po akceptacji.' },
  { q: 'Czy piszesz teksty SEO?', a: 'Tak — SEO copywriting to jedna z moich głównych specjalizacji. Research fraz (Ahrefs, Semrush), analiza SERP i intencji, optymalizacja on-page, linkowanie wewnętrzne, content clusters. Piszę teksty, które rankują w Google i jednocześnie czyta się z przyjemnością — bo wyszukiwarki nagradzają jakość, nie keyword stuffing.' },
  { q: 'W jakich formatach dostarczasz teksty?', a: 'W dowolnym: Google Docs, Word, Markdown, HTML, PDF (screen i print-ready), EPUB, MOBI, PPTX, LaTeX. Jeśli potrzebujesz gotowego produktu (ebook, katalog, prezentacja) — dostarczam plik gotowy do druku lub dystrybucji cyfrowej. Jeśli potrzebujesz wdrożenia na stronę — mogę to zrobić samodzielnie.' }
];

// ── Schema.org ──
const schema = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Person",
      "@id": "https://www.icopywriter.pl/#person",
      "name": "Karol Leszczyński",
      "jobTitle": "Copywriter",
      "url": "https://www.icopywriter.pl",
      "image": photo,
      "description": "Copywriter z 15-letnim doświadczeniem w digital marketingu. Copywriting sprzedażowy, SEO, content marketing, skład typograficzny LaTeX, redakcja, dokumentacja techniczna.",
      "knowsAbout": ["Copywriting", "SEO Copywriting", "Content Marketing", "LaTeX", "Technical Writing", "Web Development", "Email Marketing"],
      "sameAs": []
    },
    {
      "@type": "WebSite",
      "@id": "https://www.icopywriter.pl/#website",
      "url": "https://www.icopywriter.pl",
      "name": "iCopywriter.pl — Karol Leszczyński, Copywriter",
      "publisher": { "@id": "https://www.icopywriter.pl/#person" }
    },
    {
      "@type": "WebPage",
      "@id": "https://www.icopywriter.pl/#webpage",
      "url": "https://www.icopywriter.pl",
      "name": "Copywriter Karol Leszczyński — Copywriting, SEO, Content, Skład tekstu",
      "isPartOf": { "@id": "https://www.icopywriter.pl/#website" },
      "about": { "@id": "https://www.icopywriter.pl/#person" },
      "description": "Copywriter z 15+ lat doświadczenia. Copywriting sprzedażowy, SEO copywriting, content marketing, skład typograficzny LaTeX, redakcja, dokumentacja techniczna. Od słowa po gotowy produkt."
    },
    {
      "@type": "ProfessionalService",
      "@id": "https://www.icopywriter.pl/#service",
      "name": "Copywriter — Karol Leszczyński",
      "provider": { "@id": "https://www.icopywriter.pl/#person" },
      "serviceType": ["Copywriting", "SEO Copywriting", "Content Marketing", "Technical Writing", "Typesetting", "Editing"],
      "areaServed": "PL"
    },
    {
      "@type": "FAQPage",
      "mainEntity": faqs.map(f => ({
        "@type": "Question",
        "name": f.q,
        "acceptedAnswer": { "@type": "Answer", "text": f.a }
      }))
    }
  ]
};
---

<BaseLayout
  title="Copywriter Karol Leszczyński — Copywriting, SEO, Content, Skład tekstu | iCopywriter.pl"
  description="Copywriter z 15-letnim doświadczeniem. Copywriting sprzedażowy, SEO copywriting, content marketing, skład typograficzny LaTeX, redakcja i korekta, dokumentacja techniczna. Od słowa po gotowy produkt — ebooki, katalogi, strony, bazy wiedzy."
  keywords="copywriter, copywriting, copywriter Polska, SEO copywriter, copywriter freelancer, content writer, copywriting sprzedażowy, pisanie tekstów, teksty na stronę, copywriter cennik"
  schema={schema}
  canonical="https://www.icopywriter.pl/"
>

  <!-- ════════════════════════════════════════
       HERO
       ════════════════════════════════════════ -->
  <section class="hero" aria-labelledby="hero-title">
    <div class="hero__bg" aria-hidden="true">
      <div class="hero__grain"></div>
      <div class="hero__gradient"></div>
    </div>
    <div class="container hero__grid">
      <div class="hero__content">
        <div class="hero__badge">
          <span class="hero__badge-dot"></span>
          Copywriter · Content · Skład tekstu
        </div>

        <h1 id="hero-title" class="hero__title">
          Piszę teksty, które<br><em>pracują na Twój biznes</em>
        </h1>

        <p class="hero__subtitle">
          Jestem copywriterem z&nbsp;15-letnim doświadczeniem w&nbsp;digital marketingu. Tworzę
          treści sprzedażowe, SEO i&nbsp;eksperckie — i&nbsp;jako jeden z&nbsp;nielicznych dostarczam
          je w&nbsp;formie gotowych produktów: ebooków ze&nbsp;składem typograficznym, katalogów
          print-ready, stron z&nbsp;wdrożonymi tekstami. Od&nbsp;pierwszego słowa po&nbsp;finalny plik.
        </p>

        <div class="hero__actions">
          <a href="#kontakt" class="hero__cta-primary">
            Porozmawiajmy o Twoim projekcie
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 8h10M9 4l4 4-4 4"/></svg>
          </a>
          <a href="#uslugi" class="hero__cta-secondary">Co mogę dla Ciebie zrobić?</a>
        </div>

        <div class="hero__proof">
          <span>✓ 15+ lat doświadczenia</span>
          <span>✓ Copy + skład + development</span>
          <span>✓ Wycena w 24h</span>
        </div>
      </div>

      <div class="hero__aside">
        <div class="hero__card">
          <div class="hero__card-header">
            <span class="hero__card-mark">KL</span>
            <div class="hero__card-meta">
              <span class="hero__card-name">Karol Leszczyński</span>
              <span class="hero__card-title">copywriter · content · skład</span>
            </div>
          </div>
          <div class="hero__card-divider"></div>
          <div class="hero__card-stack">
            <div class="hero__card-row">
              <span class="hero__card-label">Piszę</span>
              <span class="hero__card-value">copy sprzedażowy, SEO, ekspercki</span>
            </div>
            <div class="hero__card-row">
              <span class="hero__card-label">Redaguję</span>
              <span class="hero__card-value">merytoryka, styl, korekta, proofreading</span>
            </div>
            <div class="hero__card-row">
              <span class="hero__card-label">Składam</span>
              <span class="hero__card-value">LaTeX, Quarto, PDF, EPUB, druk</span>
            </div>
            <div class="hero__card-row">
              <span class="hero__card-label">Koduję</span>
              <span class="hero__card-value">Astro, Next.js, React, Node.js</span>
            </div>
            <div class="hero__card-row">
              <span class="hero__card-label">Wdrażam</span>
              <span class="hero__card-value">AWS, CI/CD, docs-as-code</span>
            </div>
          </div>
          <div class="hero__card-divider"></div>
          <div class="hero__card-footer">
            <span class="hero__card-stat"><strong>15+</strong> lat</span>
            <span class="hero__card-stat"><strong>1500+</strong> stron w&nbsp;jednym projekcie</span>
            <span class="hero__card-stat"><strong>end‑to‑end</strong> delivery</span>
          </div>
        </div>
        <p class="hero__aside-caption">Od pierwszego słowa po gotowy produkt — bez pośredników.</p>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       INTRO — What is a copywriter (SEO block)
       ════════════════════════════════════════ -->
  <section class="intro" aria-labelledby="intro-title">
    <div class="container">
      <div class="intro__inner reveal">
        <div class="intro__eyebrow">Czym zajmuje się copywriter?</div>
        <h2 id="intro-title" class="intro__title">
          Copywriter to ktoś, kto zamienia słowa<br>w&nbsp;<em>wymierne rezultaty biznesowe</em>
        </h2>
        <div class="intro__columns">
          <div class="intro__col">
            <p>
              <strong>Copywriter</strong> pisze teksty, które mają konkretny cel — sprzedać produkt,
              pozyskać leada, zbudować zaufanie, wyjaśnić skomplikowany temat, pozycjonować markę
              w&nbsp;wyszukiwarce. To nie „pisanie dla pisania". Każdy tekst, który tworzę, odpowiada
              na pytanie: <em>co ten tekst ma osiągnąć?</em>
            </p>
            <p>
              Dobry <strong>copywriting</strong> zaczyna się od researchu — nie od pustej strony.
              Analizuję grupę docelową, badam konkurencję, sprawdzam frazy kluczowe, mapuję ścieżkę
              klienta. Dopiero potem siadam do pisania. I&nbsp;potem redaguję. I&nbsp;potem redaguję
              jeszcze raz — bo prosty, czytelny tekst wymaga więcej pracy niż rozwlekły.
            </p>
          </div>
          <div class="intro__col">
            <p>
              Jako <strong>copywriter freelancer</strong> z&nbsp;ponad 15-letnim doświadczeniem widziałem
              setki branż, tysięce briefów i&nbsp;dziesiątki tysięcy napisanych stron. Wiem, co działa
              na landing page, co rankuje w&nbsp;Google, co konwertuje w&nbsp;emailu, co utrzymuje
              uwagę w&nbsp;artykule na 5000 słów.
            </p>
            <p>
              Ale to, co naprawdę mnie wyróżnia, to <strong>kompetencje wykraczające poza sam tekst</strong>.
              Składam typograficznie w&nbsp;LaTeX, projektuję layouty, koduję strony, wdrażam docs-as-code.
              Nie potrzebujesz łańcucha freelancerów: copywriter → grafik → składacz → developer.
              Potrzebujesz jednego człowieka, który zrobi to wszystko — od&nbsp;briefu po gotowy produkt.
            </p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       SERVICES
       ════════════════════════════════════════ -->
  <section class="services" id="uslugi" aria-labelledby="services-title">
    <div class="container">
      <div class="reveal">
        <SectionHeading
          label="Usługi"
          title="Co mogę dla Ciebie <em>napisać</em>?"
          subtitle="Sześć obszarów, w&nbsp;których pracuję. Każdy obejmuje pełen pipeline: research → copywriting → redakcja → skład/wdrożenie."
        />
      </div>
      <div class="services__list">
        {services.map((srv, i) => (
          <div class={`services__item reveal ${srv.accent ? 'services__item--accent' : ''}`} style={`--delay: ${i * 0.06}s`}>
            <div class="services__item-header">
              <span class="services__item-num">{srv.num}</span>
              <h3 class="services__item-title">{srv.title}</h3>
            </div>
            <p class="services__item-desc">{srv.desc}</p>
            <div class="services__item-tags">
              {srv.tags.map((tag) => (
                <span class="services__item-tag">{tag}</span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       WHAT SETS ME APART
       ════════════════════════════════════════ -->
  <section class="apart" id="przewagi" aria-labelledby="apart-title">
    <div class="container">
      <div class="reveal">
        <SectionHeading
          label="Dlaczego ja"
          title="Czym się wyróżniam na tle innych <em>copywriterów</em>?"
          subtitle="Nie jestem kolejnym copywriterem, który oddaje tekst w&nbsp;Wordzie. Oto co dostajesz, pracując ze mną."
          inverted
        />
      </div>
      <div class="apart__grid">
        {advantages.map((adv, i) => (
          <div class="apart__card reveal" style={`--delay: ${i * 0.08}s`}>
            <span class="apart__card-icon" aria-hidden="true">{adv.icon}</span>
            <h3>{adv.title}</h3>
            <p>{adv.desc}</p>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       PROCESS
       ════════════════════════════════════════ -->
  <section class="proc" id="proces" aria-labelledby="proc-title">
    <div class="container">
      <div class="reveal">
        <SectionHeading
          label="Proces"
          title="Jak pracuję — od briefu po <em>gotowy tekst</em>"
          subtitle="Przewidywalny, przejrzysty proces. Wiesz, na jakim etapie jest Twój projekt, i&nbsp;masz kontrolę na każdym kroku."
        />
      </div>
      <div class="proc__steps">
        {process.map((step, i) => (
          <div class="proc__step reveal" style={`--delay: ${i * 0.06}s`}>
            <div class="proc__step-num">{String(i + 1).padStart(2, '0')}</div>
            <div class="proc__step-content">
              <h3>{step.step}</h3>
              <p>{step.desc}</p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       PORTFOLIO
       ════════════════════════════════════════ -->
  <section class="port" id="portfolio" aria-labelledby="port-title">
    <div class="container">
      <div class="reveal">
        <SectionHeading
          label="Portfolio"
          title="Wybrane <em>realizacje</em>"
          subtitle="Od zeszytów ćwiczeń dla wydawnictwa, przez opisy produktów e-commerce, po bazy wiedzy i&nbsp;dokumentację techniczną."
        />
      </div>
      <div class="port__grid">
        {portfolio.map((item, i) => (
          <div class="port__card reveal" style={`--delay: ${i * 0.05}s`}>
            <span class="port__card-cat">{item.category}</span>
            <h3 class="port__card-title">{item.title}</h3>
            <span class="port__card-result">
              <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
              {item.result}
            </span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       ABOUT / TRUST
       ════════════════════════════════════════ -->
  <section class="about" id="o-mnie" aria-labelledby="about-title">
    <div class="container">
      <div class="about__inner reveal">
        <div class="about__text">
          <span class="about__label">O mnie</span>
          <h2 id="about-title" class="about__title">Karol Leszczyński</h2>
          <p class="about__lead">
            Copywriter, content strategist i&nbsp;technical writer z&nbsp;ponad 15-letnim
            doświadczeniem w&nbsp;digital marketingu. Specjalizuję się w&nbsp;tworzeniu treści,
            które łączą skuteczność sprzedażową z&nbsp;jakością merytoryczną i&nbsp;perfekcyjnym
            wykonaniem — od&nbsp;pierwszego zdania po gotowy produkt wydawniczy.
          </p>
          <p>
            Przez lata budowałem kompetencje, których nie spotyka się w&nbsp;jednej osobie:
            <strong>copywriting sprzedażowy i&nbsp;SEO</strong>, <strong>skład typograficzny w&nbsp;LaTeX</strong>,
            <strong>programowanie</strong> (React, Next.js, Astro, Node.js, AWS),
            <strong>redakcja merytoryczna i&nbsp;korekta</strong>. Dzięki temu dostarczam kompletne
            rozwiązania: piszę tekst, redaguję go, składam typograficznie, renderuję do wybranego
            formatu, wdrażam na stronę — bez pośredników.
          </p>
          <p>
            Pracowałem z&nbsp;wydawnictwami edukacyjnymi (11 zeszytów ćwiczeń, podręczniki, poradniki),
            firmami e-commerce (opisy produktów, sklepy), firmami medtech (katalogi, instrukcje),
            platformami EdTech (bazy pytań, opracowania) i&nbsp;dziesiątkami firm z&nbsp;różnych branż.
            Każdy projekt traktuję z&nbsp;takim samym zaangażowaniem — niezależnie od tego,
            czy to 5-stronicowa oferta, czy 250-stronowy ebook.
          </p>
          <div class="about__trust">
            {trustPoints.map((point) => (
              <span class="about__trust-item">
                <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
                {point}
              </span>
            ))}
          </div>
        </div>
        <div class="about__photo">
          <img src={photo} alt="Karol Leszczyński, copywriter" width="360" height="450" loading="lazy" />
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       FAQ
       ════════════════════════════════════════ -->
  <section class="faq" id="faq" aria-labelledby="faq-title">
    <div class="container">
      <div class="reveal">
        <SectionHeading
          label="FAQ"
          title="Najczęstsze pytania o&nbsp;<em>copywriterze</em>"
          subtitle="Odpowiedzi na pytania, które słyszę najczęściej — o&nbsp;pracy copywritera, cenach, procesie i&nbsp;formatach."
        />
      </div>
      <div class="faq__list">
        {faqs.map((faq, i) => (
          <details class="faq__item reveal" style={`--delay: ${i * 0.04}s`}>
            <summary class="faq__question">
              <span>{faq.q}</span>
              <svg class="faq__chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><polyline points="6 9 12 15 18 9"/></svg>
            </summary>
            <div class="faq__answer"><p>{faq.a}</p></div>
          </details>
        ))}
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       CTA / CONTACT
       ════════════════════════════════════════ -->
  <section class="cta" id="kontakt" aria-labelledby="cta-title">
    <div class="cta__bg" aria-hidden="true"></div>
    <div class="container">
      <div class="cta__inner reveal">
        <h2 id="cta-title" class="cta__title">Masz projekt? <em>Porozmawiajmy.</em></h2>
        <p class="cta__desc">
          Opisz, co potrzebujesz — cel, zakres, format, termin. Odpowiem z&nbsp;wyceną,
          propozycją podejścia i&nbsp;harmonogramem w&nbsp;ciągu 24&nbsp;godzin.
          Bezpłatnie i&nbsp;bez&nbsp;zobowiązań.
        </p>
        <div class="cta__actions">
          <a href="mailto:kontakt@icopywriter.pl" class="cta__button">
            kontakt@icopywriter.pl
            <svg width="16" height="16" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M3 8h10M9 4l4 4-4 4"/></svg>
          </a>
        </div>
        <div class="cta__meta">
          <span>✓ Wycena w 24 godziny</span>
          <span>✓ Bez zobowiązań</span>
          <span>✓ Faktura VAT</span>
        </div>
      </div>
    </div>
  </section>

  <!-- ════════════════════════════════════════
       SEO CONTENT
       ════════════════════════════════════════ -->
  <section class="seo" aria-label="Copywriter — informacje">
    <div class="container-narrow">
      <article class="seo__article">
        <h2>Copywriter — kim jest i&nbsp;czym się zajmuje?</h2>
        <p>
          <strong>Copywriter</strong> to specjalista od tworzenia tekstów marketingowych, reklamowych,
          sprzedażowych i&nbsp;informacyjnych. Zakres pracy copywritera obejmuje pisanie treści na strony
          internetowe, artykułów blogowych, opisów produktów, emaili marketingowych, postów w social media,
          ebooków, white papers, katalogów, prezentacji, materiałów szkoleniowych i&nbsp;dokumentacji
          technicznej. Współczesny <strong>copywriting</strong> to znacznie więcej niż samo pisanie — to
          połączenie strategii marketingowej, analizy SEO, psychologii perswazji i rzemiosła językowego.
        </p>

        <h3>Copywriter freelancer — elastyczność bez kompromisów</h3>
        <p>
          <strong>Copywriter freelancer</strong> to niezależny specjalista, który pracuje projektowo
          z&nbsp;firmami z&nbsp;różnych branż. Zaletą współpracy z&nbsp;copywriterem freelancerem jest
          elastyczność, bezpośredni kontakt, szybki czas reakcji i&nbsp;często niższe koszty niż
          zatrudnienie agencji. Jako copywriter freelancer z&nbsp;ponad 15-letnim doświadczeniem oferuję
          dodatkową przewagę: kompetencje wykraczające poza sam tekst — skład typograficzny w&nbsp;LaTeX,
          programowanie stron, redakcję merytoryczną i&nbsp;korektę. Jeden punkt kontaktu zamiast
          koordynacji wielu podwykonawców.
        </p>

        <h3>SEO copywriting — teksty, które rankują w&nbsp;Google</h3>
        <p>
          <strong>SEO copywriting</strong> to specjalizacja copywritera polegająca na tworzeniu treści
          zoptymalizowanych pod wyszukiwarki, które jednocześnie czyta się z&nbsp;przyjemnością. Dobry
          <strong>SEO copywriter</strong> łączy research fraz kluczowych z&nbsp;wiedzą o intencji
          wyszukiwania, architekturze treści, linkowaniu wewnętrznym i&nbsp;optymalizacji on-page.
          Efekt: artykuły i&nbsp;podstrony, które budują widoczność organiczną, generują ruch
          i&nbsp;konwertują odwiedzających w&nbsp;klientów.
        </p>

        <h3>Ile kosztuje copywriter?</h3>
        <p>
          Ceny usług <strong>copywritera</strong> zależą od rodzaju tekstu, złożoności tematu, objętości
          i&nbsp;zakresu prac. Typowe widełki: artykuł blogowy (3000–5000 słów) 600–2000 zł, teksty
          na podstronę 500–2000 zł, opisy produktów od 50 zł za sztukę (pakiety), ebook od 3000 zł,
          strategia contentowa od 3000 zł. Wycena konkretnego projektu jest zawsze bezpłatna
          i&nbsp;niezobowiązująca — wystarczy opisać cel, zakres i&nbsp;termin.
        </p>
      </article>
    </div>
  </section>

</BaseLayout>

<style>
  /* ════════════════════════════════════════
     HERO
     ════════════════════════════════════════ */
  .hero { position: relative; padding: calc(72px + var(--space-12)) 0 var(--space-16); overflow: hidden; min-height: 85vh; display: flex; align-items: center; }
  .hero__bg { position: absolute; inset: 0; z-index: 0; }
  .hero__gradient { position: absolute; inset: 0; background: radial-gradient(ellipse 60% 60% at 75% 60%, rgba(26,107,71,0.05) 0%, transparent 55%), radial-gradient(ellipse 50% 50% at 10% 20%, rgba(12,31,45,0.03) 0%, transparent 50%); }
  .hero__grain { position: absolute; inset: 0; opacity: 0.02; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E"); }

  .hero__grid { position: relative; z-index: 1; display: grid; grid-template-columns: 1fr 380px; gap: var(--space-10); align-items: center; }
  @media (max-width: 900px) { .hero__grid { grid-template-columns: 1fr; } }

  .hero__badge { display: inline-flex; align-items: center; gap: 8px; font-family: var(--font-mono); font-size: 0.68rem; font-weight: var(--fw-semibold); letter-spacing: 0.08em; text-transform: uppercase; color: var(--accent); margin-bottom: var(--space-5); opacity: 0; animation: heroFadeUp 0.8s ease 0.1s forwards; }
  .hero__badge-dot { width: 7px; height: 7px; background: var(--accent); border-radius: 50%; animation: heroPulse 2.5s ease-in-out infinite; }
  @keyframes heroPulse { 0%, 100% { opacity: 1; } 50% { opacity: 0.4; } }

  .hero__title { font-family: var(--font-display); font-size: clamp(2.4rem, 5.5vw, 3.8rem); font-weight: var(--fw-bold); line-height: 1.08; letter-spacing: -0.02em; color: var(--ink); opacity: 0; animation: heroFadeUp 0.9s ease 0.25s forwards; }
  .hero__title em { font-style: italic; color: var(--accent); }

  .hero__subtitle { font-size: clamp(0.95rem, 1.5vw, 1.05rem); line-height: var(--lh-relaxed); color: var(--ink-muted); margin-top: var(--space-5); max-width: 560px; opacity: 0; animation: heroFadeUp 0.9s ease 0.4s forwards; }

  .hero__actions { display: flex; align-items: center; gap: var(--space-5); flex-wrap: wrap; margin-top: var(--space-8); opacity: 0; animation: heroFadeUp 0.9s ease 0.55s forwards; }
  .hero__cta-primary { display: inline-flex; align-items: center; gap: 10px; padding: 14px 28px; background: var(--accent); color: #fff; font-size: var(--fs-sm); font-weight: var(--fw-semibold); border-radius: var(--radius-sm); transition: all var(--transition-base); }
  .hero__cta-primary:hover { background: var(--accent-hover); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(26,107,71,0.25); }
  .hero__cta-primary svg { transition: transform var(--transition-fast); }
  .hero__cta-primary:hover svg { transform: translateX(3px); }
  .hero__cta-secondary { font-size: var(--fs-sm); font-weight: var(--fw-medium); color: var(--ink-muted); border-bottom: 1px solid var(--rule); padding-bottom: 2px; transition: all var(--transition-fast); }
  .hero__cta-secondary:hover { color: var(--accent); border-color: var(--accent); }

  .hero__proof { display: flex; gap: var(--space-5); flex-wrap: wrap; margin-top: var(--space-8); opacity: 0; animation: heroFadeUp 0.8s ease 0.7s forwards; }
  .hero__proof span { font-family: var(--font-mono); font-size: 0.66rem; color: var(--ink-faint); letter-spacing: 0.02em; }

  @keyframes heroFadeUp { from { opacity: 0; transform: translateY(18px); } to { opacity: 1; transform: translateY(0); } }

  /* Card aside */
  .hero__aside { opacity: 0; animation: heroFadeUp 1s ease 0.5s forwards; }
  @media (max-width: 900px) { .hero__aside { max-width: 380px; margin-top: var(--space-4); } }

  .hero__card {
    background: var(--card-bg); border: 1px solid var(--rule-light);
    border-radius: var(--radius-md); padding: var(--space-5) var(--space-5);
    box-shadow: var(--shadow-lg);
    position: relative; overflow: hidden;
  }
  .hero__card::before {
    content: ''; position: absolute; top: 0; left: 0; right: 0; height: 3px;
    background: linear-gradient(90deg, var(--accent), var(--accent-light));
  }

  .hero__card-header { display: flex; align-items: center; gap: var(--space-3); margin-bottom: var(--space-4); }
  .hero__card-mark {
    width: 40px; height: 40px; display: flex; align-items: center; justify-content: center;
    font-family: var(--font-display); font-size: 0.95rem; font-weight: var(--fw-bold);
    color: #fff; background: var(--accent); border-radius: var(--radius-sm);
    flex-shrink: 0;
  }
  .hero__card-meta { display: flex; flex-direction: column; }
  .hero__card-name { font-family: var(--font-display); font-size: var(--fs-sm); font-weight: var(--fw-semibold); color: var(--ink); }
  .hero__card-title { font-family: var(--font-mono); font-size: 0.56rem; color: var(--ink-faint); letter-spacing: 0.06em; text-transform: uppercase; }

  .hero__card-divider { height: 1px; background: var(--rule-light); margin: var(--space-3) 0; }

  .hero__card-stack { display: flex; flex-direction: column; gap: var(--space-2); }
  .hero__card-row { display: grid; grid-template-columns: 72px 1fr; gap: var(--space-3); align-items: baseline; }
  .hero__card-label { font-family: var(--font-mono); font-size: 0.6rem; font-weight: var(--fw-semibold); color: var(--accent); text-transform: uppercase; letter-spacing: 0.06em; text-align: right; }
  .hero__card-value { font-size: var(--fs-sm); color: var(--ink-muted); line-height: var(--lh-snug); }

  .hero__card-footer { display: flex; flex-wrap: wrap; gap: var(--space-4); }
  .hero__card-stat { font-family: var(--font-mono); font-size: 0.62rem; color: var(--ink-faint); letter-spacing: 0.02em; }
  .hero__card-stat strong { color: var(--accent); font-weight: var(--fw-bold); }

  .hero__aside-caption {
    margin-top: var(--space-3); text-align: center;
    font-family: var(--font-display); font-size: var(--fs-xs);
    font-style: italic; color: var(--ink-faint);
  }

  /* ════════════════════════════════════════
     INTRO
     ════════════════════════════════════════ */
  .intro { padding: var(--space-24) 0; border-top: 1px solid var(--rule-light); }

  .intro__eyebrow { font-family: var(--font-mono); font-size: 0.62rem; font-weight: var(--fw-semibold); letter-spacing: 0.12em; text-transform: uppercase; color: var(--accent); margin-bottom: var(--space-4); }

  .intro__title { font-family: var(--font-display); font-size: clamp(1.6rem, 3.5vw, 2.4rem); font-weight: var(--fw-semibold); line-height: var(--lh-snug); color: var(--ink); margin-bottom: var(--space-8); max-width: 700px; }
  .intro__title em { font-style: italic; color: var(--accent); }

  .intro__columns { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-8); }
  @media (max-width: 768px) { .intro__columns { grid-template-columns: 1fr; } }

  .intro__col p { font-size: var(--fs-sm); line-height: var(--lh-relaxed); color: var(--ink-muted); margin-bottom: var(--space-4); }
  .intro__col strong { color: var(--ink); font-weight: var(--fw-semibold); }
  .intro__col em { font-style: italic; }

  /* ════════════════════════════════════════
     SERVICES
     ════════════════════════════════════════ */
  .services { padding: var(--space-24) 0; background: var(--paper-warm); border-top: 1px solid var(--rule-light); }

  .services__list { display: flex; flex-direction: column; }

  .services__item { padding: var(--space-6) 0; border-bottom: 1px solid var(--rule-light); transition-delay: var(--delay, 0s); }
  .services__item:first-child { border-top: 1px solid var(--rule); }

  .services__item--accent { background: var(--accent-muted); margin: 0 calc(-1 * var(--space-5)); padding-left: var(--space-5); padding-right: var(--space-5); border-left: 3px solid var(--accent); border-radius: 0 var(--radius-sm) var(--radius-sm) 0; }

  .services__item-header { display: flex; align-items: baseline; gap: var(--space-4); margin-bottom: var(--space-2); }
  .services__item-num { font-family: var(--font-mono); font-size: 0.62rem; color: var(--accent); font-weight: var(--fw-semibold); letter-spacing: 0.04em; flex-shrink: 0; }
  .services__item-title { font-family: var(--font-display); font-size: var(--fs-xl); font-weight: var(--fw-semibold); color: var(--ink); line-height: var(--lh-snug); }

  .services__item-desc { font-size: var(--fs-sm); line-height: var(--lh-relaxed); color: var(--ink-muted); max-width: 680px; margin-bottom: var(--space-3); }

  .services__item-tags { display: flex; flex-wrap: wrap; gap: 6px; }
  .services__item-tag { font-family: var(--font-mono); font-size: 0.58rem; font-weight: var(--fw-medium); color: var(--ink-faint); background: var(--card-bg); border: 1px solid var(--rule-light); padding: 3px 8px; border-radius: var(--radius-sm); }

  /* ════════════════════════════════════════
     APART (dark)
     ════════════════════════════════════════ */
  .apart { padding: var(--space-24) 0; background: var(--navy); position: relative; overflow: hidden; }
  .apart::before { content: ''; position: absolute; inset: 0; opacity: 0.03; background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 256 256' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='0.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E"); }

  .apart__grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-5); position: relative; }
  @media (max-width: 768px) { .apart__grid { grid-template-columns: 1fr; } }

  .apart__card { border: 1px solid rgba(255,255,255,0.06); border-radius: var(--radius-md); padding: var(--space-6); transition: border-color 0.3s ease; transition-delay: var(--delay, 0s); }
  .apart__card:hover { border-color: rgba(255,255,255,0.12); }

  .apart__card-icon { display: block; font-size: 1.3rem; color: var(--accent-light); margin-bottom: var(--space-3); }

  .apart__card h3 { font-family: var(--font-display); font-size: var(--fs-lg); font-weight: var(--fw-semibold); color: #fff; margin-bottom: var(--space-2); line-height: var(--lh-snug); }
  .apart__card p { font-size: var(--fs-sm); line-height: var(--lh-relaxed); color: rgba(255,255,255,0.45); }

  /* ════════════════════════════════════════
     PROCESS
     ════════════════════════════════════════ */
  .proc { padding: var(--space-24) 0; }

  .proc__steps { display: flex; flex-direction: column; }

  .proc__step { display: grid; grid-template-columns: 48px 1fr; gap: var(--space-5); padding: var(--space-5) 0; border-bottom: 1px solid var(--rule-light); transition-delay: var(--delay, 0s); }
  .proc__step:first-child { border-top: 1px solid var(--rule); }

  .proc__step-num { font-family: var(--font-mono); font-size: 0.68rem; font-weight: var(--fw-bold); color: var(--accent); text-align: right; padding-top: 3px; }

  .proc__step-content h3 { font-family: var(--font-display); font-size: var(--fs-lg); font-weight: var(--fw-semibold); color: var(--ink); margin-bottom: var(--space-2); line-height: var(--lh-snug); }
  .proc__step-content p { font-size: var(--fs-sm); line-height: var(--lh-relaxed); color: var(--ink-muted); max-width: 600px; }

  /* ════════════════════════════════════════
     PORTFOLIO
     ════════════════════════════════════════ */
  .port { padding: var(--space-24) 0; background: var(--paper-warm); border-top: 1px solid var(--rule-light); }

  .port__grid { display: grid; grid-template-columns: repeat(2, 1fr); gap: var(--space-4); }
  @media (max-width: 640px) { .port__grid { grid-template-columns: 1fr; } }

  .port__card { background: var(--card-bg); border: 1px solid var(--rule-light); border-radius: var(--radius-md); padding: var(--space-5); transition: border-color var(--transition-base), transform var(--transition-base); transition-delay: var(--delay, 0s); }
  .port__card:hover { border-color: var(--accent); transform: translateY(-2px); }

  .port__card-cat { display: block; font-family: var(--font-mono); font-size: 0.56rem; font-weight: var(--fw-semibold); letter-spacing: 0.1em; text-transform: uppercase; color: var(--accent); margin-bottom: var(--space-2); }

  .port__card-title { font-family: var(--font-display); font-size: var(--fs-base); font-weight: var(--fw-semibold); color: var(--ink); line-height: var(--lh-snug); margin-bottom: var(--space-3); }

  .port__card-result { display: inline-flex; align-items: center; gap: 5px; font-family: var(--font-mono); font-size: 0.62rem; font-weight: var(--fw-semibold); color: var(--ink-muted); }
  .port__card-result svg { color: var(--accent); }

  /* ════════════════════════════════════════
     ABOUT
     ════════════════════════════════════════ */
  .about { padding: var(--space-24) 0; }

  .about__inner { display: grid; grid-template-columns: 1fr 320px; gap: var(--space-12); align-items: start; }
  @media (max-width: 900px) { .about__inner { grid-template-columns: 1fr; } }

  .about__label { display: inline-block; font-family: var(--font-mono); font-size: 0.62rem; font-weight: var(--fw-semibold); letter-spacing: 0.12em; text-transform: uppercase; color: var(--accent); margin-bottom: var(--space-3); }

  .about__title { font-family: var(--font-display); font-size: clamp(1.8rem, 3.5vw, 2.6rem); font-weight: var(--fw-bold); color: var(--ink); margin-bottom: var(--space-5); }

  .about__lead { font-size: var(--fs-lg); line-height: var(--lh-relaxed); color: var(--ink-soft); margin-bottom: var(--space-5); }

  .about__text p { font-size: var(--fs-sm); line-height: var(--lh-relaxed); color: var(--ink-muted); margin-bottom: var(--space-4); }
  .about__text strong { color: var(--ink); font-weight: var(--fw-semibold); }

  .about__trust { display: flex; flex-direction: column; gap: var(--space-2); margin-top: var(--space-6); padding-top: var(--space-6); border-top: 1px solid var(--rule-light); }
  .about__trust-item { display: flex; align-items: center; gap: 8px; font-size: var(--fs-sm); color: var(--ink-muted); }
  .about__trust-item svg { color: var(--accent); flex-shrink: 0; }

  .about__photo { border-radius: var(--radius-md); overflow: hidden; box-shadow: var(--shadow-lg); border: 1px solid var(--rule-light); }
  @media (max-width: 900px) { .about__photo { max-width: 280px; } }
  .about__photo img { width: 100%; height: auto; display: block; aspect-ratio: 4/5; object-fit: cover; object-position: top center; }

  /* ════════════════════════════════════════
     FAQ
     ════════════════════════════════════════ */
  .faq { padding: var(--space-24) 0; background: var(--paper-warm); border-top: 1px solid var(--rule-light); }

  .faq__list { max-width: 740px; display: flex; flex-direction: column; }
  .faq__item { border-bottom: 1px solid var(--rule-light); transition-delay: var(--delay, 0s); }
  .faq__item:first-child { border-top: 1px solid var(--rule); }
  .faq__question { display: flex; align-items: center; justify-content: space-between; gap: var(--space-4); padding: var(--space-5) 0; cursor: pointer; font-family: var(--font-display); font-size: var(--fs-lg); font-weight: var(--fw-semibold); color: var(--ink); line-height: var(--lh-snug); list-style: none; transition: color var(--transition-fast); }
  .faq__question::-webkit-details-marker { display: none; }
  .faq__question::marker { display: none; content: ''; }
  .faq__question:hover { color: var(--accent); }
  .faq__chevron { flex-shrink: 0; color: var(--ink-faint); transition: transform 0.3s ease, color 0.3s ease; }
  .faq__item[open] .faq__chevron { transform: rotate(180deg); color: var(--accent); }
  .faq__answer { padding: 0 0 var(--space-6); }
  .faq__answer p { font-size: var(--fs-sm); line-height: var(--lh-relaxed); color: var(--ink-muted); max-width: 620px; }

  /* ════════════════════════════════════════
     CTA
     ════════════════════════════════════════ */
  .cta { padding: var(--space-24) 0; position: relative; overflow: hidden; background: var(--paper); }
  .cta__bg { position: absolute; inset: 0; background: radial-gradient(ellipse 70% 60% at 70% 50%, rgba(26,107,71,0.04) 0%, transparent 60%); }
  .cta__inner { max-width: 600px; margin: 0 auto; text-align: center; position: relative; }
  .cta__title { font-family: var(--font-display); font-size: clamp(1.8rem, 3.5vw, 2.6rem); font-weight: var(--fw-bold); line-height: var(--lh-snug); color: var(--ink); margin-bottom: var(--space-4); }
  .cta__title em { font-style: italic; color: var(--accent); }
  .cta__desc { font-size: var(--fs-base); color: var(--ink-muted); line-height: var(--lh-relaxed); margin-bottom: var(--space-8); }
  .cta__actions { margin-bottom: var(--space-6); }
  .cta__button { display: inline-flex; align-items: center; gap: 10px; padding: 14px 32px; background: var(--accent); color: #fff; font-size: var(--fs-base); font-weight: var(--fw-semibold); border-radius: var(--radius-sm); transition: all var(--transition-base); }
  .cta__button:hover { background: var(--accent-hover); transform: translateY(-2px); box-shadow: 0 8px 24px rgba(26,107,71,0.25); }
  .cta__button svg { transition: transform var(--transition-fast); }
  .cta__button:hover svg { transform: translateX(3px); }
  .cta__meta { display: flex; justify-content: center; gap: var(--space-5); flex-wrap: wrap; }
  .cta__meta span { font-family: var(--font-mono); font-size: 0.66rem; color: var(--ink-faint); }

  /* ════════════════════════════════════════
     SEO CONTENT
     ════════════════════════════════════════ */
  .seo { padding: var(--space-20) 0; border-top: 1px solid var(--rule-light); }
  .seo__article h2 { font-family: var(--font-display); font-size: var(--fs-2xl); font-weight: var(--fw-semibold); margin-bottom: var(--space-6); color: var(--ink); }
  .seo__article h3 { font-family: var(--font-display); font-size: var(--fs-xl); font-weight: var(--fw-semibold); margin-top: var(--space-10); margin-bottom: var(--space-3); color: var(--ink); }
  .seo__article p { font-size: 0.92rem; line-height: var(--lh-relaxed); color: var(--ink-muted); margin-bottom: var(--space-4); }
  .seo__article strong { color: var(--ink-soft); font-weight: var(--fw-medium); }
</style>